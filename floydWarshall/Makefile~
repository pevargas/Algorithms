#Which compiler?
CC = g++
#Which flags?
CFLAGS = -Wall
#Which files?
SOURCES = pvargas-assign5.cpp
OBJECTS = $(SOURCES: .cpp = .o)
#Name of program
EXECUTABLE = trans

all: $(EXECUTABLE)

$(EXECUTABLE): $(SOURCES)
	$(CC) $(CFLAGS) $(SOURCES) -o $@

clean:
	rm -rf *o $(EXECUTABLE)